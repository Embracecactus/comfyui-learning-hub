# 剩余费用证据的执行条件（2026-09-21）

公开价目与当前匿名中国站目录已经实际采集，不再从头重复调研。先读 `06-公开报价与目录复核.md`；244项目录、26/26显式2K视频价表、6次H3匿名报价及GitHub快照新鲜度已留存。当前工作是账户账单与租价取证，不是再建一套客户端。

## 授权与安全入口

通过仓库Actions的Secret输入 `RH_API_KEY`（消费级）和 `RH_ENTERPRISE_API_KEY`（企业共享）或本机受控环境注入，不在对话、源码、日志和产物里提交明文Key。当前两个约定输入均为空；这不代表枚举了所有Secrets或读取了用户本机。企业Key只用于它获准访问的接口，不尝试权限绕过。

读账户状态、免费price-preview和旧taskId不创建视频。新增付费测试须先取得明确人民币上限及可用RH币限额；没有额度授权时仍为0，不充值、不升级、不购买GPU。源链已有taskId优先复用；提交结果未知不自动重提。

## 需要的实际证据

消费级：先只读重查三条历史5秒2K样本；再在授权预算内分别生成10/15秒、取usage和媒体数据。模型费与RH币分列，不用5秒附加币做固定或线性外推。

企业共享：以企业Key取得精确参数preview，再运行代表性直接2K任务和同源768P→regeneration两阶段任务。记录两个taskId、实际最终模型提示词哈希、源媒体哈希、每阶段真实计费秒与usage。公开阶段价0.48与0.30元/输出秒只是报价，不得自动填入实扣字段。两阶段各计费5/10/15秒时的预算为3.90/7.80/11.70元；真实计费秒和舍入仍以平台账单为准。

会员/独占：读取登录后的官方币购买或会员账单、计费定义及GPU租期报价。把赠币、充值币、固定会员费、租金及产量分摊区别记录。公开页面此次重定向登录是已确认的访问条件，不等于证明存在一个统一RH币汇率，也不等于租价为0。

## 验收

复用 `rh_evidence_audit.py`，不重复搭建服务。全部脱敏证据进入可追溯输入，再由统一生成器更新文档与表格。按证据类型分别关闭公开报价、权限、真实生成、实扣、完整两阶段账单五种状态。重复运行构建必须一致；预算、匿名网页计算结果和合成测试数据均不可冒充企业或个人实扣账单。
