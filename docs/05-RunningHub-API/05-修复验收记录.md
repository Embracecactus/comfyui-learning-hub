# 2026-09-21 修复验收记录

基线：main 的 `3e2e4c43e778e41a9bc577edd8ee3d25ad095471`。范围仅 RunningHub API、成本和交付可复现性；不修改本地模型实现、不充值或替用户创建企业资源。

## 第一轮已执行的工程检查

| 检查 | 结果 |
|---|---|
| 新增 handoff 离线回归 | 50 项通过，无网络和真实Key |
| `tests/scripts/test_*.py` | 102 项通过，包含上述50项，不重复计数 |
| `tests/custom_nodes/test_*.py` | 20 项通过 |
| 第一轮合计 | 122 项通过 |
| 两个兼容生成入口 `--check` | 均通过，生成结果与统一输入一致 |
| 五个Shell入口 `bash -n` | 通过 |
| 01/02/04/05 Shell默认执行 | 无Key dry-run通过，未发出生成请求 |
| 两阶段Shell | 语法及源链相关单元测试通过；没有真实源媒体，不声称已完成再生成 |
| 只读采集器缺Key分支 | 返回 `blocked_missing_key`，请求数0，新付费任务数0 |

## 第一轮关键修复

价格、历史用量、权益和缺口归到 `data/handoff-evidence.json`；CSV/注册表/Markdown/HTML从同一生成器导出。模型/价格快照日期与本次维护日期分离，显式刷新记录真实输入SHA256；不再伪称自动获取了新价格。第一轮注册表保留快照中的422个模型、228个视频、22个2K标签端点，直接文生候选5个（其中有价格缺口），而不是22条“提示词直出已验证”。本轮实时网站范围见下节。

客户端补account及video入口、区分flat/legacy响应、预估失败阻止默认付费、网络结果未知不盲重试、失败媒体检查不冒充成功、应用参数回显脱敏。旧T2V脚本默认读取已提交注册表，不再依赖 `output/` 下的本地developer-kit。Shell入口复用统一客户端。

再生成工具检查源媒体约束与实际最终提示词，持久化源任务和哈希关联；只读采集器及账单对账器不创建付费任务。所有测试账单均为合成样例，不能作为平台实测。

## 第一轮费用证据边界（历史记录）

第一轮未执行企业price-preview或重查私人任务；09-17价格和三次个人成功仅从已有仓库报告迁入。第一轮当时保留的“公开价格新鲜度”缺口，随后由下节当天公开页面采集推进；实际扣费、私人账单、币兑人民币与独占租金不因此关闭。

## 第二轮：当天公开价目与目录取证

本节新增事实以 `06-公开报价与目录复核.md` 和绑定哈希的 `data/public-pricing-20260921.json` 为准，不覆盖上文第一轮的历史验证状态。

在中国站匿名页面完成244/244标准模型SKU详情读取；26个显式2K视频端点的价表全部取得，合计保留60份与候选/上下游有关的公开价表。GitHub原始422模型和353价格文件重新下载，SHA256均未变化；当前网页视图和旧GitHub集合分别保存，并有完整差集，不把未出现在当前视图判定为下架。

H3文生768P/2K × 5/10/15秒共6组匿名网页报价已核实。文生再生成当前公开价为0.30元/再生成输出秒，按双方计费时长相等时两阶段预算0.78元/秒；不能再用原厂参考价算1.10元/秒，也不能把多模态参考素材的收费套到文生生成源视频上。RH普通超分2K公开价0.35元/秒，向上取整且至少5秒；5.167秒源片按6秒预算。

新增19项公开证据离线回归（含1项公开示例签名脱敏检查）。本地全量脚本121项、自定义节点20项，共141项通过；两个生成入口`--check`通过。远端发布执行35575935938也完成导入、生成、测试与提交。测试覆盖匿名报价与实际扣费分离、限定视图的目录去重/分页（当前244项）、26/26目标价表、Token+秒混合计费不得压扁、再生成不重复收费、超分取整/最低时长、缺失凭据状态和生成一致性。测试仅检查已取得的公开响应及本地算法，不是新增付费视频实测。

本轮实际发现的阻塞：约定的`RH_API_KEY`、`RH_ENTERPRISE_API_KEY`两个CI Secret输入均为空；本地亦无RH环境凭据。官方会员入口跳到SSO登录，独占/共享控制台与完整节点费页同样需登录。私人查询0、付费任务0，未充值、未购买会员/企业资源。企业实扣、两阶段账单、个人10/15秒币费、会员/币兑元成本和独占租金不因此关闭。

公开schema的示例媒体链接含临时签名/凭据形态字段，导入时统一脱敏。保留原始响应哈希，不发布签名URL，不绕过GitHub推送保护。临时写入workflow已移除，保留的CI和手动公开取证workflow均为只读。
