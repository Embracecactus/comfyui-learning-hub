diff --git a/comfy/utils.py b/comfy/utils.py
--- a/comfy/utils.py
+++ b/comfy/utils.py
@@ -125,7 +125,21 @@ def load_torch_file(ckpt, safe_load=False, device=None, return_metadata=False):
     metadata = None
     if ckpt.lower().endswith(".safetensors") or ckpt.lower().endswith(".sft"):
         try:
-            if comfy.memory_management.aimdo_enabled:
+            if DISABLE_MMAP:
+                # On Windows, safe_open().get_tensor() can crash inside
+                # torch_cpu.dll for very large (>10 GiB) text encoders before
+                # the copy below has a chance to bypass mmap. safetensors 0.8
+                # provides a real non-mmap backend for this case.
+                sd = safetensors.torch.load_file(ckpt, device=device.type, backend="pread")
+                if return_metadata:
+                    # safe_open() itself creates a Windows file mapping. A very
+                    # large CLIP can therefore still fail with WinError 1455
+                    # even though tensor loading already uses pread. Read only
+                    # the small JSON header directly instead.
+                    with open(ckpt, "rb") as f:
+                        header_size = struct.unpack("<Q", f.read(8))[0]
+                        metadata = json.loads(f.read(header_size)).get("__metadata__", {})
+            elif comfy.memory_management.aimdo_enabled:
                 sd, metadata = load_safetensors(ckpt)
                 if not return_metadata:
                     metadata = None
@@ -134,8 +148,6 @@ def load_torch_file(ckpt, safe_load=False, device=None, return_metadata=False):
                     sd = {}
                     for k in f.keys():
                         tensor = f.get_tensor(k)
-                        if DISABLE_MMAP:  # TODO: Not sure if this is the best way to bypass the mmap issues
-                            tensor = tensor.to(device=device, copy=True)
                         sd[k] = tensor
                     if return_metadata:
                         metadata = f.metadata()
